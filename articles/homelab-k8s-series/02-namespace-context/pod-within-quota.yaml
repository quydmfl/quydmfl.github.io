apiVersion: v1
kind: Pod
metadata:
  name: within-quota
  namespace: dev
spec:
  containers:
    - name: app
      image: nginx:1.27-alpine
      resources:
        requests:
          cpu: "200m"
          memory: "128Mi"
        limits:
          cpu: "300m"
          memory: "256Mi"
