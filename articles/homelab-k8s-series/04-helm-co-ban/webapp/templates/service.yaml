apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-webapp
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-webapp
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
